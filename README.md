steps to run my todo project
first go to the terminal and move to the root
>cd todo
then run the json
>json-server --watch db.json --port 5000

then again open the new terminal and move to root
>cd todo
and run the project
>npm start 